# ===============================================
# Alembic configuration (stub for JuicyFox Plan A)
# ===============================================
# ВНИМАНИЕ: Alembic миграции в этом проекте пока не используются.
# Этот файл оставлен как заготовка для будущего перехода
# на PostgreSQL или использования миграций с SQLite.
#
# По умолчанию база данных находится в /app/data/juicyfox.sqlite
# ===============================================

[alembic]
# Путь к папке с миграциями (создастся позже при необходимости)
script_location = shared/db/migrations

# URL базы данных (по умолчанию SQLite)
sqlalchemy.url = sqlite:////app/data/juicyfox.sqlite

# Опциональные параметры
# timezone = UTC
# truncate_slug_length = 40
# version_path_separator = os  # Используется для вложенных папок версий

# Настройки логирования (можно оставить закомментированными)
#[loggers]
#keys = root,sqlalchemy,alembic

#[handlers]
#keys = console

#[formatters]
#keys = generic

#[logger_root]
#level = WARN
#handlers = console

#[logger_sqlalchemy]
#level = WARN
#handlers =
#qualname = sqlalchemy.engine

#[logger_alembic]
#level = INFO
#handlers =
#qualname = alembic

#[handler_console]
#class = StreamHandler
#args = (sys.stderr,)
#level = NOTSET
#formatter = generic

#[formatter_generic]
#format = %(levelname)-5.5s [%(name)s] %(message)s
